@model List<ProductDto>
@{
    ViewData["Title"] = "Today's Deals";
}

<div class="container-fluid px-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb breadcrumb-custom">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
            <li class="breadcrumb-item active" aria-current="page">Today's Deals</li>
        </ol>
    </nav>

    <div class="d-flex align-items-center mb-3">
        <i class="fas fa-tags fs-2 text-danger me-2"></i>
        <h4 class="mb-0">Today's Deals</h4>
    </div>

    @if (Model.Any())
    {
        <div class="row g-3">
            @foreach (var product in Model)
            {
                <div class="col-6 col-md-4 col-lg-3">
                    <partial name="_ProductCard" model="product" />
                </div>
            }
        </div>
    }
    else
    {
        <div class="text-center py-5">
            <i class="fas fa-tags fa-4x text-muted mb-3"></i>
            <h4>No deals available right now</h4>
            <p class="text-muted">Check back soon for new offers</p>
        </div>
    }
</div>
